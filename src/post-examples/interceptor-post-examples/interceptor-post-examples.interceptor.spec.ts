import { InterceptorPostExamplesInterceptor } from './interceptor-post-examples.interceptor';

describe('InterceptorPostExamplesInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorPostExamplesInterceptor()).toBeDefined();
  });
});
