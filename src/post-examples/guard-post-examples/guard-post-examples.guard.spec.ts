import { GuardPostExamplesGuard } from './guard-post-examples.guard';

describe('GuardPostExamplesGuard', () => {
  it('should be defined', () => {
    expect(new GuardPostExamplesGuard()).toBeDefined();
  });
});
